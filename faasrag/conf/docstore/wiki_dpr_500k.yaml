name: wiki_dpr_500k

# Source corpus (truth)
source_uri: s3://vectorindexes/wiki-dpr/wiki-passages/500k/meta_psgs_w100_nq_no_index_500000_text.jsonl
source_format: jsonl

# Field mapping inside JSONL
source_id_key: faiss_id
source_title_key: title
source_text_key: text

# 'sqlite', 'jsonl_offsets' for simple local file with offsets, 'memory' for in-memory (not recommended for large corpora)
backend_kind: memory # sqlite, jsonl_offsets, memory

