name: dpr_multiset

passage_encoder: sentence-transformers/facebook-dpr-ctx_encoder-multiset-base
question_encoder: sentence-transformers/facebook-dpr-question_encoder-multiset-base
metric: ip
normalize: false
dim: 768

# formatting is slightly less “proven” than NQ in your experiments,
# but this is the most common DPR passage string convention:
# passage_format: "title [SEP] text"
